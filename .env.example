# Gmail SMTP for the contact form (Vercel / local).
# Copy to .env.local for local dev. Add the same keys in Vercel → Project → Settings → Environment Variables.
# Never commit real passwords.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=masjidalathar@gmail.com
SMTP_PASS=your-gmail-app-password-here
SMTP_FROM=masjidalathar@gmail.com
SMTP_TO=masjidalathar@gmail.com
